import 'jest-extended';

beforeAll(async () => {
  // Add something here
}, 30000);

beforeEach(async () => {
  // Add something here
});

afterAll(async () => {
  // Add something here
}, 30000);

afterEach(async () => {
  // Add something here
});
